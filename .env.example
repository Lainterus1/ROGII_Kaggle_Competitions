# Copy to .env for local-only settings. Do not commit .env.

# Optional Kaggle API location if used locally.
# KAGGLE_CONFIG_DIR=C:/Users/<user>/.kaggle

# Optional MLflow tracking URI. Defaults to local ./mlruns when unset.
# MLFLOW_TRACKING_URI=
