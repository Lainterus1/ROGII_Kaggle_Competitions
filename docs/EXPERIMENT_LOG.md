# Experiment Log

## Purpose

Provide a human-readable experiment history complementary to MLflow.

## Owns

Run summaries, local CV scores, public leaderboard scores entered manually, feature set names, model types, configs, notes and follow-up ideas.

## Update when

- A baseline or experiment is run.
- A Kaggle public leaderboard score is recorded.
- An experiment changes the next-step plan.

## Do not store here

- Raw MLflow artifact directories.
- Large prediction files.
- Secrets or Kaggle credentials.

## Current content

Template for future entries:

| Date | Run name | Model | Features | CV metric | Public LB | Config | Notes |
|---|---|---|---|---|---|---|---|
| 2026-06-04 | naive_last_known_tvt_input | Rule baseline | Last non-null pre-PS `TVT_input` per well | RMSE `15.909853` on train post-PS rows | Not submitted | `configs/baseline_naive.yaml` | Generated and validated `outputs/submission.csv`; no MLflow run because this is a non-model smoke baseline |
| 2026-06-04 | baseline_lgbm_safe_numeric | LightGBM | MD, X, Y, Z, GR, GR_is_missing, MD_delta, MD_relative, row_position | RMSE `120.06 ± 11.31` (GroupKFold 5) | Not submitted | `configs/baseline_lgbm.yaml` | Pure geometric baseline; deliberately excludes TVT_input to measure geometry-only signal |
| 2026-06-04 | baseline_lgbm_tvt | LightGBM | safe_numeric_v1 + last_tvt_input (well-level constant) | RMSE `20.84 ± 3.24` (GroupKFold 5, local) | Not submitted | `configs/baseline_lgbm.yaml` | 5.8x improvement over geometry-only; local run on 3.78M post-PS rows, 773 wells |
| 2026-06-05 | baseline_lgbm_tvt_kaggle | LightGBM | safe_numeric_v1 + last_tvt_input | RMSE `20.58 ± 3.99` (GroupKFold 5, Kaggle) | **24.114** | `configs/baseline_lgbm.yaml` | First Kaggle submission. Offline notebook via `rogii-repo` dataset. LB/CV gap +3.5. |
| 2026-06-05 | r1_lgbm_residual_geo_gr | LightGBM | safe_numeric_v1 + 9 geometry + 20 GR (38 total), residual delta target | RMSE `14.09 ± 0.88` (GroupKFold 5, local, delta space) | Not submitted | `configs/baseline_lgbm.yaml` | Roadmap R1 first run. 32% improvement over Stage 4. Target: `TVT - last_tvt_input`. |
| 2026-06-05 | r1_lgbm_optimized | LightGBM | 6 base + 9 geometry + 3 GR (18 total), residual delta target | RMSE `14.19 ± 0.89` (GroupKFold 5, local) | **12.247** | `configs/baseline_lgbm.yaml` | R1 Kaggle submission. CV→LB: 14.19→12.247 (−2.0 gap, LB BETTER than CV). 49% improvement over Stage 4 LB (24.11→12.25). After feature ablation: 20 zero/low-importance features removed with zero CV regression. 18 features: MD, X, Y, Z, GR, MD_relative + 9 geometry + gr_roll_mean_101, gr_roll_std_101, gr_energy. Fixed offline workflow submission `53410572` also scored `12.247`. |
| 2026-06-05 | r2_lgbm_typewell_v1 | LightGBM | R1 features + 15 typewell (53 total), residual delta target | RMSE `14.75 ± 0.77` (GroupKFold 5, local, delta space) | Not submitted | `configs/baseline_lgbm.yaml` | Roadmap R2. Typewell features degraded CV (+0.66 vs R1). Likely cause: `tw_gr_residual_*` ≈ `GR - const`, highly correlated with base `GR`, adding noise without new signal. Typewell summary features (`tw_range`, `tw_gr_mean`, `tw_gr_std`) are well-level constants already partially captured by residual approach. **Decision: do not promote R2; submit R1 to Kaggle.** |
| 2026-06-05 | a1_lgbm_trajectory | LightGBM | R1 features + 5 trajectory + 1 dogleg (24→23 total), residual delta target | RMSE `14.24 ± 0.82` (GroupKFold 5, local) | **12.487** | `configs/baseline_lgbm.yaml` + `--include-trajectory` | Stage A1. CV flat (+0.05 vs R1 14.19), LB worse (+0.24 vs R1 12.247). All 5 trajectory features are r >= 0.99 with existing geometry features (z_local_delta≈dz_since_ps, dip_angle≈dzdmd, sin_azimuth≈dydmd, cos_azimuth≈dxdmd). No new signal — only importance redistribution across duplicates. `dogleg_severity_10m` ablated mid-experiment (importance 0.00). **Decision: REJECTED. Revert to R1 as active baseline.** |
| 2026-06-05 | a2a_lgbm_dwt | LightGBM | R1 (18) + 2 DWT = 20 features, residual delta target | CV `14.13 ± 0.77` (GroupKFold 5, local) | LB **12.558** `53412072` | `configs/a2_lgbm.yaml` (DWT only) | Stage A2a. Causal trailing-window GR DWT (db4, window=256). CV +0.06 vs R1 (14.19) but LB **+0.311 worse** vs R1 (12.247). DWT features don't generalize to test set despite causal windows and GroupKFold. **Decision: SUPERSEDED by R1.** |
| 2026-06-05 | a2b_lgbm_spatial | LightGBM | R1 (18) + 9 spatial KNN = 27 features, residual delta target | RMSE `14.21 ± 0.81` (GroupKFold 5, local) | Not submitted | `configs/a2_lgbm.yaml` (spatial only) | Stage A2b. Strict OOF spatial KNN (k=5,10,50) with ball_tree. Reference: pre-PS TVT_input from OOF wells only. No leakage (CV not implausibly low). Flat CV (-0.02 vs R1). Spatial features add no new signal beyond X/Y/Z. **Decision: NOT PROMOTED (code kept).** |
| 2026-06-05 | a2_lgbm_combined | LightGBM | R1 (18) + 2 DWT + 9 spatial = 29 features, residual delta target | RMSE `14.19 ± 0.74` (GroupKFold 5, local) | Not submitted | `configs/a2_lgbm.yaml` | Stage A2 combined. CV flat vs R1. Spatial neutralizes DWT's marginal gain. **Not promoted as combined; A2a (DWT-only) is the promoted config.** |
| 2026-06-05 | a3a_lgbm_dtw | LightGBM | A2a (20) + 2 DTW = 22 features, residual delta target | RMSE `14.63 ± 1.14` (GroupKFold 5, local) | Not submitted | DWT+DTW CLI flags | Stage A3a. DTW typewell alignment (Sakoe-Chiba window=50) on pre-PS GR. CV +0.50 vs A2a (14.13). Median GR cross-correlation 0.43 — DTW forces alignment without real signal. Fold 5 RMSE 16.5 vs fold 3 RMSE 13.1 (high variance). **Decision: REJECTED.** |
| 2026-06-05 | a3b1_signed_log | LightGBM | A2a (20), signed-log residual target | RMSE `14.64` (GroupKFold 3, local) | Not submitted | N/A | Stage A3b.1. Signed-log: sign(x)*log1p(abs(x)). Residuals bounded ±40, skew 0.74 — not heavy-tailed. +0.51 vs A2a. **Decision: REJECTED.** |
| 2026-06-05 | a3b2_derivative | LightGBM | A2a (20), derivative dTVT/dMD target | RMSE `14.32` (GroupKFold 3, local) | Not submitted | N/A | Stage A3b.2. Predict derivative + integrate to TVT. +0.19 vs A2a (14.13). Integration error accumulation. **Decision: REJECTED.** |
| 2026-06-05 | a4_geology | LightGBM | A2a (20) + 7 geology = 27 features, residual delta target | RMSE `14.57 ± 0.80` (GroupKFold 5, local) | Not submitted | DWT+Geology CLI flags | Geology v1: well-level constants (form at PS, next form, boundaries). Signal unstable: folds 1,3 improved (−0.5), folds 2,4,5 degraded (+1.0). **Decision: REJECTED.** |
| 2026-06-05 | a4_geology_v2 | LightGBM | A2a (20) + 9 per-row GR z-scores = 29 features, residual delta target | RMSE `14.17 ± 0.86` (GroupKFold 5, local) | Not submitted | DWT+Geology CLI flags | Geology v2: per-row GR-vs-formation z-scores (8 formations + rolling). More stable than v1 but flat CV (−0.04 vs A2a 14.13). Tabular feature ceiling confirmed at ~14.13. **Decision: NOT PROMOTED. Code kept.** |
| 2026-06-06 | a2a_kaggle_packaged | LightGBM | 20 features, residual delta target, identical to promoted A2a | CV `14.13 ± 0.77` | LB **12.558** `53412072` | Kernel `00-rogii-inference-a2a-dwt` v1 | A2a Kaggle packaging. LB 12.558 vs R1 12.247 (+0.311). CV improvement (+0.06) inverted on LB — DWT doesn't generalize. R1 remains active baseline. **Decision: NOT PROMOTED.** |
| 2026-06-06 | b1_lgbm_beam | LightGBM | R1 (18) + 19 beam search + 2 DWT = 39 features, residual delta target | 5-fold: CV `14.43 ± 0.51`; 3-fold: `14.38 ± 0.83` | Not submitted | `configs/b1_lgbm.yaml` | Stage B1 Beam Search Stratigraphic Alignment. `beam_std` is #2 feature by importance (6.2%) but cannibalizes X/Y/Z importance (X: 17→5.6%, Y: 15→5.5%, Z: 12.5→6.0%). Beam-only (no geometry/GR): CV `16.02` — worse than naive baseline (15.91). Beam-minimal (beam+DWT, no geometry/GR): CV `16.02`. Same pattern as typewell/DTW/geology — typewell-referenced features redistribute without net gain. **Decision: REJECTED. Tabular ceiling CV ~14.1 confirmed.** |
| 2026-06-06 | b1_beam_only | LightGBM | R1 (18) + 19 beam, no DWT = 37 features, residual delta target | CV `14.39 ± 0.80` (3-fold) | Not submitted | `configs/b1_beam_only.yaml` | B1 without DWT. CV identical to B1 full (14.38). DWT not the issue — beam itself doesn't add net signal. |
| 2026-06-06 | b1_beam_minimal | LightGBM | 6 base + 19 beam, no geometry/GR/DWT = 25 features, residual delta target | CV `16.02 ± 0.83` (3-fold) | Not submitted | `configs/b1_beam_minimal.yaml` | B1 beam-only (no geometry/GR features). CV worse than naive baseline (15.91). Beam cannot replace spatial geometry features. |
| 2026-06-06 | b2_slope_recent | LightGBM | R1 (18), residual delta, slope_recent baseline (last 200 MD rows) | CV `14.16 ± 0.97` (3-fold) | Not submitted | `configs/b2_lgbm.yaml` | Best non-flat baseline. CV flat vs R1 (14.19). Fold variance high (13.2–15.5). Local slope near PS does not improve predictions. |
| 2026-06-06 | b2_slope_md | LightGBM | R1 (18), residual delta, slope_md baseline (global MD trend) | CV `284 ± 26` (3-fold) | Not submitted | `configs/b2_lgbm.yaml` | Catastrophic. Global TVT-vs-MD trend in known zone does NOT continue into evaluation zone — wells change direction after PS. Polyfit slope экстраполирует неверный тренд. |
| 2026-06-06 | b2_wls | LightGBM | R1 (18), residual delta, WLS baseline (exp-weighted recent MD) | CV `130 ± 8` (3-fold) | Not submitted | `configs/b2_lgbm.yaml` | Also catastrophic. Exponential weighting of recent rows does not fix the TVT-vs-MD non-linearity after PS. |
| 2026-06-06 | b2_verdict | — | All B2b slope methods rejected | Best CV 14.16 (flat vs R1) | Not submitted | — | TVT-vs-MD trend from known zone does NOT linearly extrapolate into evaluation zone. Flat baseline remains optimal. **B2b REJECTED.** Code kept in `src/rogii/baseline.py`, `src/rogii/smoothing.py`. |
| 2026-06-06 | b3_lgbm_fplane | LightGBM | R1 (18) + 21 formation plane KNN = 39 features, residual delta | CV `14.99 ± 0.73` (3-fold) | Not submitted | `configs/b3_lgbm.yaml` | Stage B3 Formation Plane KNN. `fp_knn_mean_dist` #5 (5.0%), `fp_nearest_dist` #6 (4.5%). FP features take ~35% importance from X/Y/Z (X: 17→3.7%, Y: 15→3.4%, Z: 12.5→3.3%). Same cannibalization pattern as B1. **Decision: REJECTED.** Code kept. |
| 2026-06-06 | prp2_z_drift_3fold | LightGBM | R1 (18) + 3 z-drift = 21 features, residual delta | CV `14.61 +- 0.81` (3-fold) vs flat `14.17` | Not submitted | -- | PrP2 Z-Drift Physics (3-fold). Fold 2 shows +0.97 degradation. Phase 0 confirmed 2/3 features are linear duplicates (implied_tvt = Z+const, resid = dz_since_ps). |
| 2026-06-06 | prp2_z_drift | LightGBM | R1 (18) + 3 z-drift = 21 features, residual delta | CV `14.20 +- 0.64` (5-fold) vs flat `14.19` | Not submitted | -- | PrP2 Z-Drift Physics (5-fold). CV flat (+0.01). Fold inconsistency: fold 2 -0.80, fold 3 +0.62. Only offset_at_anchor is new signal; the other 2 are duplicates. Well-level constant doesn't help tree splits. **Decision: NOT PROMOTED.** Code kept behind include_z_drift flag. |
| 2026-06-06 | prp3_postproc_impl | — | Post-processing pipeline (Savgol + TVT clipping) | Not run yet | Not submitted | -- | PrP3 implementation complete: `apply_postprocessing()` (clip → smooth chain), `compute_tvt_clip_bounds()`, `--eval-postproc` CV flag, per-well visualization, 17 tests. **Pending: CV evaluation with `--eval-postproc`.** |
| 2026-06-06 | prp3_savgol_submit | LightGBM (R1, 18 features) + Savgol w=31 p=2 | Post-processing: per-well Savgol smoothing | OOF `14.2123` vs raw `14.2187` (−0.0064) | LB **12.239** `53428554` | Kernel `00-rogii-inference-r1` v8, offline, `--savgol-smooth` | **NEW BEST LB!** −0.008 vs R1 (12.247). Savgol improvement confirmed on both CV and LB. OOF→LB: 14.2123→12.239 (−1.97 gap, consistent with R1 gap of −1.94). Savgol w=31 p=2 is now the recommended default post-processing. |
| 2026-06-07 | a4_multiseed | LightGBM (R1, 18 features) | 3-seed ensemble [42,7,123], delta target | CV `14.052 ± 0.868` (GroupKFold 5) vs single `14.191 ± 0.887` | Not submitted | `configs/a4_multiseed.yaml` | Stage A4 multi-seed averaging. Mean +0.139, std −0.019. Seed correlation high (~0.95+). Models benefit from averaging but are not fully independent. 3x training time. |
| 2026-06-07 | a4_strat_default | LightGBM (R1, 18 features) | StratifiedGroupKFold (4,5), single seed 42, delta target | CV `13.806 ± 1.522` (StratifiedGroupKFold 5) vs GroupKFold `14.191 ± 0.887` | Not submitted | `configs/a4_stratified.yaml` | Best single-seed strat config. Mean −0.385, std +0.635. 33 classes, min=5 wells per class. Higher std reflects fold heterogeneity — potentially better CV/LB correlation. |
| 2026-06-07 | a4_strat_3_3 | LightGBM (R1, 18 features) | StratifiedGroupKFold (3,3), single seed 42, delta target | CV `14.195 ± 1.010` | Not submitted | CLI | Too coarse (18 classes). CV mean identical to GroupKFold. |
| 2026-06-07 | a4_strat_5_5 | LightGBM (R1, 18 features) | StratifiedGroupKFold (5,5), single seed 42, delta target | CV `13.990 ± 1.150` | Not submitted | CLI | 38 classes. In between (3,3) and (4,5). |
| 2026-06-07 | a4_strat_multiseed | LightGBM (R1, 18 features) | StratifiedGroupKFold (4,5) + 3-seed ensemble [42,7,123], delta target | CV `13.763 ± 1.523` | Not submitted | CLI | Best overall CV. Strat(4,5) + multi-seed: −0.428 vs GroupKFold single-seed, −0.289 vs GroupKFold multi-seed.

## Open questions

- None.
